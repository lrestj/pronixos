require("tfm").setup({
	replace_netrw = true,
  enable_cmds = true,
  -- Customise UI. The below options are the default
  ui = {
    border = "rounded",
    height = 1,
    width = 0.7,
    x = 0.2,
    y = 0.2,
  },
})
